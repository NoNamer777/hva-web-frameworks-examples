package nl.hva.springbasics.sort;

public interface SortAlgorithm {

    /**
     * Destructive sort: It changes the order of the original array
     * @param numbers
     */
    void sort(int [] numbers);
}
